# Problem Statement

We have a dark store which has, say, some fashion inventory already and we are creating a chatbot which will have the following flow. As soon as you come in, the chatbot will offer you three things based on whatever info they have about you, about the current trends, and the intersection with what is available in the dark store. Then you'll probably see three things shown to you, like how Rufus shows Amazon some stuff. In Amazon there's Rufus, which shows these patterns inside the chat, but you can click and directly go to the page or it'll give you the option to probably think through and ask questions or something like "I'm looking for something else". Then the bot will say, "Are you looking for?" Then they'll say, "I'm looking for ABC, ABC." What that experience will be and what will be surfaced to them?

That is the mock application we need to create. Probably we will create a desktop app because that will be showing it better. We'll probably use GPT 4.0 kind of an API. Jipper API should be playing this part but mostly we'll have a backend, all context, some artificial data, and some stuff in the backend which will enable all this. This is what we need to create. It's a lot of product ideation and thinking about what can help us break better.
